Zodiac Sign Generator 

A simple Python program that calculates a user's zodiac sign based on their date of birth and saves the result to a CSV file.

 Features

- Accepts user name input
- Validates date of birth format (`dd-mm-yyyy`)
- Calculates zodiac sign
- Saves user data to `zodiac_data.csv`
- Uses `pandas` for data storage

